package controller

// Discord-related controllers are handled via the OAuth flow (oauth/provider.go)
// and the Midjourney relay (relay/mjproxy_handler.go). Add Discord-specific
// endpoint handlers here as needed.
